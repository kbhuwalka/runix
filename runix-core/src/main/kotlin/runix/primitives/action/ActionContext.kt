package runix.primitives.action

/**
 * Context object passed into all Actions to provide helper functions and internal state.
 */
class ActionContext {
    // TODO: Add useful helpers like `waitUntil(...)`, access to system state, etc.
}